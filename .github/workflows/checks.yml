name: Check Software

on: workflow_dispatch

jobs:
    check:
        runs-on: ubuntu-latest 
        steps:
            - name: Output date
              run: date
            - name: Test curl
              run: curl --version
            - name: Test wget
              run: wget --version
